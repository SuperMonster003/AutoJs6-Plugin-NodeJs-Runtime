{
  "name": "js-plugin-ui",
  "type": "node",
  "main": "main.cjs",
  "node": {
    "backend": "embedded",
    "timeoutMs": 15000,
    "permissions": ["plugins", "plugin.org.example.sample_ui", "ui"]
  },
  "example": {
    "capabilities": ["plugins", "plugin.org.example.sample_ui", "ui"],
    "expectedProvider": "JS plugin manager plus the host UI provider behind the ui permission",
    "packagedSupport": "not applicable: exported APKs do not bundle this runtime; run the project from the host entry",
    "securityLimitations": ["no plugin downloads", "native plugin archives denied", "UI handles stay opaque", "without the host UI provider the ui step prints ERR_AUTOJS6_BRIDGE_PERMISSION_DENIED"]
  }
}
