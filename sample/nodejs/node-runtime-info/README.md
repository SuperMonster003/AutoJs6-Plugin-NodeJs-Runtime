# node-runtime-info

Runtime descriptor example for Phase 9 native modernization checks.

- Capabilities: Node runtime version and feature-shape reporting
- Expected provider: embedded Node runtime descriptor
- Packaged support: not part of the packaged smoke set
- Security limitations: runtime selection remains build-controlled; projects cannot load arbitrary native runtimes

Expected output is listed in `expected-output.txt`.
