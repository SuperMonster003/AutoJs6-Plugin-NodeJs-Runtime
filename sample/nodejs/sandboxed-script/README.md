# sandboxed-script

Sandboxed execution policy example for Phase 9.

- Capabilities: future brokered isolated-process execution mode
- Expected provider: hardened isolated runtime policy gate
- Packaged support: not supported for user code in this phase
- Security limitations: sandboxed scripts must use brokered capabilities and cannot access direct app permissions

Expected output is listed in `expected-output.txt`.
