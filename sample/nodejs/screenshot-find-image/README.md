# screenshot-find-image

Screenshot and image bridge example.

The script tries to capture the screen with existing permission, creates a tiny template image under the scoped project directory, and calls `findImage`. If screen capture permission is unavailable, it exits cleanly with a skipped marker.

Expected output is listed in `expected-output.txt`.
