# fs-scoped

Scoped filesystem example.

The script writes and reads files under the project working directory. It does not use absolute paths or `..` escapes.

Expected output is listed in `expected-output.txt`.
