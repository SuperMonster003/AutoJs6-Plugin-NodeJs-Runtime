# cpu-profile

Design-gated CPU profile example for future inspector-owned profiling.

- Capabilities: profiling policy probe
- Expected provider: no release profiling export
- Packaged support: not part of the packaged smoke set
- Security limitations: no `.cpuprofile` export in the current release profile

Expected output is listed in `expected-output.txt`.
