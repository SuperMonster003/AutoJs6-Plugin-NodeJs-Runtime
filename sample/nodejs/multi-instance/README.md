# multi-instance

Multi-instance runtime pool policy example for Phase 9.

- Capabilities: process pool and multi-instance policy shape
- Expected provider: design-gated embedded process pool
- Packaged support: not part of the packaged smoke set
- Security limitations: independent executions must not share V8 environments, permissions, or resource registries

Expected output is listed in `expected-output.txt`.
