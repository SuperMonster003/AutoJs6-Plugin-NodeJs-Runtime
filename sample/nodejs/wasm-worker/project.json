{
  "name": "wasm-worker",
  "type": "node",
  "main": "main.cjs",
  "node": {
    "backend": "embedded",
    "timeoutMs": 10000,
    "permissions": []
  },
  "example": {
    "capabilities": ["wasm", "worker"],
    "expectedProvider": "stable native worker_threads compute isolation",
    "packagedSupport": "supported by the three-ABI runtime kit",
    "securityLimitations": ["workers cannot access AutoJs bridge modules or raw Android objects", "WASM work remains bounded by worker resource and cleanup policy"]
  }
}
