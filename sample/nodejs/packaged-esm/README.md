# packaged-esm

Packaged ESM example using a CommonJS entry that imports a local `.mjs` module.

- Capabilities: `esm`
- Expected provider: packaged Embedded Node ESM loader
- Packaged support: supported for local project files
- Security limitations: local project graph only; no remote imports; disabled builtins stay denied

Expected output is listed in `expected-output.txt`.
