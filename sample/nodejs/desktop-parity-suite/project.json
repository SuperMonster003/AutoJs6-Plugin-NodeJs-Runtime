{
  "name": "desktop-parity-suite",
  "type": "node",
  "main": "main.cjs",
  "node": {
    "backend": "embedded",
    "timeoutMs": 15000,
    "permissions": []
  },
  "example": {
    "capabilities": ["esm", "worker_threads", "http", "raw_network", "fs.advanced", "wasm", "inspector", "pure_js_npm"],
    "expectedProvider": "Node 24 builtins as shipped by the runtime; every snippet prints its real outcome",
    "packagedSupport": "not applicable: exported APKs do not bundle this runtime; run the project from the host entry",
    "securityLimitations": ["node:wasi is denied by decision; pure WebAssembly is available", "the inspector needs an explicit Debug host request and prints its denial code otherwise", "registry installs happen in the host terminal (npm); the runtime loads checked-in or host-installed packages", "dgram and http2 need the raw_network capability"]
  }
}
