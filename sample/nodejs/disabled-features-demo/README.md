# disabled-features-demo

Safe Node Profile denial example.

The script probes dangerous or unsupported capabilities and prints their stable denial codes.

Expected output is listed in `expected-output.txt`.
