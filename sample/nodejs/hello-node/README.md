# hello-node

Minimal Embedded Node script for AutoJs6.

Run `main.cjs` as a Node project. It prints the active Node version and a stable pass marker.

Expected output is listed in `expected-output.txt`.
