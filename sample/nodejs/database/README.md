# database

SQLite bridge example using the AutoJs6 `database` module.

- Capabilities: `database`
- Expected provider: app-private Android SQLite bridge
- Packaged support: packaged live authority is future gated
- Security limitations: database names are scoped; `ATTACH`, `DETACH`, extension loading, and `node:sqlite` are denied

Expected output is listed in `expected-output.txt`.
