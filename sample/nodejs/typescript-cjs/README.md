# typescript-cjs

Lightweight TypeScript CommonJS example for AutoJs6 Node Profile v1.1.

The `.cts` entry uses erasable type syntax only. AutoJs6 strips the types at
runtime and runs the result as CommonJS.
