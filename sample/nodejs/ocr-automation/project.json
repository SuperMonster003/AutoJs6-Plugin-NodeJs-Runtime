{
  "name": "ocr-automation",
  "type": "node",
  "main": "main.cjs",
  "node": {
    "backend": "embedded",
    "timeoutMs": 180000,
    "permissions": ["screen_capture", "ocr", "image"]
  },
  "example": {
    "capabilities": ["screen_capture", "ocr", "image"],
    "expectedProvider": "Android OCR provider over opaque image handles",
    "packagedSupport": "future gated until packaged OCR model policy is accepted",
    "securityLimitations": ["opaque image handles only", "no raw Bitmap or OCR engine objects", "provider timeout applies"]
  }
}
