# npm-pure-js-cjs

Pure JavaScript CommonJS npm-style package example.

The project carries tiny fixture packages in `node_modules` to demonstrate the supported no-install workflow.

Expected output is listed in `expected-output.txt`.
