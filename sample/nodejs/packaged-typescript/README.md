# packaged-typescript

Packaged-compatible TypeScript module graph example.

The `.mts` entry imports a local `.ts` module and dynamically imports a local
`.mts` module. It is intended to stay compatible with packaged APK project
assets and encrypted moduleSources.
