# cjs-package

CommonJS package resolution example.

This project loads a local helper and a small package from `node_modules` without running `npm install`.

Expected output is listed in `expected-output.txt`.
