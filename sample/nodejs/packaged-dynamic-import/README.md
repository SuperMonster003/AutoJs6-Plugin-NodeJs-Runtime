# packaged-dynamic-import

Packaged dynamic import example for a local `.mjs` feature module.

- Capabilities: `dynamic_import`, `esm`
- Expected provider: packaged Embedded Node local dynamic import loader
- Packaged support: supported for relative local files
- Security limitations: URL, absolute-path, and project-escape imports stay denied

Expected output is listed in `expected-output.txt`.
