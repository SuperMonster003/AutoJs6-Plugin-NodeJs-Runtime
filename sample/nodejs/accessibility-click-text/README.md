# accessibility-click-text

Accessibility bridge example.

The script checks whether accessibility is enabled. If it is enabled, it tries to click a text selector. If it is disabled, it exits cleanly with a skipped marker.

Expected output is listed in `expected-output.txt`.
