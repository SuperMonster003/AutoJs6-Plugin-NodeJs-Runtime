# npm-ci-pure-js

This fixture verifies the desktop-assisted `npm ci` path. It keeps pure JavaScript packages under `vendor/`, requires
`package-lock.json`, and intentionally does not check in `node_modules`.
