# hardened-sandbox

Design-gated hardened sandbox example.

- Capabilities: future isolated-process sandbox policy
- Expected provider: no user-code launch route in v1.1
- Packaged support: not part of the packaged smoke set
- Security limitations: `nodeSandboxed` is policy-only until the brokered route is implemented

Expected output is listed in `expected-output.txt`.
