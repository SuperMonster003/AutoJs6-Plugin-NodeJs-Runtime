# AutoJs6 Node.js Runtime

This plugin provides `libnode.so` for AutoJs6 Node.js scripts. Install and enable it in the AutoJs6 plugin center when the main app is built with the Node.js plugin runtime profile.
